# =====================================================
# VARIABLES DE ENTORNO - APP PARA BRILLAR BACKEND
# =====================================================

# Configuración del servidor
PORT=3000
NODE_ENV=development

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=app_para_brillar
DB_CHARSET=utf8mb4

# JWT Tokens
JWT_SECRET=tu_jwt_secret_super_seguro_aqui
JWT_REFRESH_SECRET=tu_jwt_refresh_secret_super_seguro_aqui

# CORS - Orígenes permitidos
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

# AWS S3 (para almacenamiento de fotos)
AWS_ACCESS_KEY_ID=tu_aws_access_key
AWS_SECRET_ACCESS_KEY=tu_aws_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=app-para-brillar-photos

# Gemini AI (Google)
GEMINI_API_KEY=tu_gemini_api_key

# OpenAI (opcional, para análisis avanzado)
OPENAI_API_KEY=tu_openai_api_key

# Redis (opcional, para caché)
REDIS_URL=redis://localhost:6379

# Email (opcional, para notificaciones)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_email@gmail.com
SMTP_PASS=tu_password_de_aplicacion

# Monitoreo
SENTRY_DSN=tu_sentry_dsn

# =====================================================
# INSTRUCCIONES DE CONFIGURACIÓN
# =====================================================

# 1. Copia este archivo como .env
# cp env.example .env

# 2. Modifica las variables según tu entorno:
# - DB_PASSWORD: Contraseña de tu MySQL
# - JWT_SECRET: Genera una clave segura (puedes usar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
# - JWT_REFRESH_SECRET: Otra clave segura diferente
# - AWS_*: Si usas AWS S3 para fotos
# - GEMINI_API_KEY: Para el chat con IA
# - OPENAI_API_KEY: Para análisis avanzado (opcional)

# 3. Para desarrollo local, solo necesitas:
# - DB_* (configuración de base de datos)
# - JWT_* (claves de autenticación)
# - ALLOWED_ORIGINS (orígenes permitidos)

# 4. Para producción, configura todas las variables necesarias 